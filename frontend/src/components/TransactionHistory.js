import React from 'react';
import { Card } from 'react-bootstrap';

const TransactionHistory = ({ groupId }) => {
  // This component can be expanded to show a detailed transaction history
  // For now, it's a placeholder that works with ExpenseList
  return null;
};

export default TransactionHistory;